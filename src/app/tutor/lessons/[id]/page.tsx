"use client";

import LessonDetails from "../../../lessons/[id]/page";

// This is a wrapper component that reuses the main lesson details component
export default function TutorLessonDetails() {
  return <LessonDetails />;
} 